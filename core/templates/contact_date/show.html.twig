{% extends 'base.html.twig' %}

{% block title %}ContactDate{% endblock %}

{% block body %}
    <h1>ContactDate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_date.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ contact_date.date ? contact_date.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ contact_date.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contact_date_index') }}">back to list</a>

    <a href="{{ path('app_contact_date_edit', {'id': contact_date.id}) }}">edit</a>

    {{ include('contact_date/_delete_form.html.twig') }}
{% endblock %}
