deptrac:
  paths:
    - src
  layers:
    - name: Service
      collectors:
        - type: classNameRegex
          value: '#^App\\Service\\.*#'
    - name: Command
      collectors:
        - type: classNameRegex
          value: '#^App\\Command\\.*#'
    - name: ApiResource
      collectors:
        - type: classNameRegex
          value: '#^App\\ApiResource\\.*#'
    - name: Controller
      collectors:
        - type: classNameRegex
          value: '#^App\\Controller\\.*#'
    - name: Entity
      collectors:
        - type: classNameRegex
          value: '#^App\\Entity\\.*#'
    - name: Repository
      collectors:
        - type: classNameRegex
          value: '#^App\\Repository\\.*#'
    - name: Form
      collectors:
        - type: classNameRegex
          value: '#^App\\Form\\.*#'
    - name: Security
      collectors:
        - type: classNameRegex
          value: '#^App\\Security\\.*#'
    - name: State
      collectors:
        - type: classNameRegex
          value: '#^App\\State\\.*#'
    - name: Doctrine
      collectors:
        - type: classNameRegex
          value: '#^App\\Doctrine\\.*#'
    - name: OpenApi
      collectors:
        - type: classNameRegex
          value: '#^App\\OpenApi\\.*#'
    - name: Vendor
      collectors:
        - type: bool
          must:
            - type: classNameRegex
              value: '#^(Symfony|Doctrine|ApiPlatform)\\.*#'
  ruleset:
    Service:
      - Entity
      - Repository
      - Security
      - Vendor
      - Doctrine
    Command:
      - Entity
      - Service
      - Vendor
      - ApiResource
    ApiResource:
      - Entity
      - Service
      - Vendor
      - State
      - Controller
    State:
      - Entity
      - Security
      - Vendor
      - Service
      - ApiResource
    Controller:
      - Entity
      - Form
      - Security
      - Service
      - Vendor
      - ApiResource
    Entity:
      - Repository
      - Security
      - Vendor
      - Doctrine
    Repository:
      - Entity
      - Vendor
    Form:
      - Entity
      - Vendor
    Security:
      - Entity
      - Vendor

    Doctrine:
      - Entity
      - Security
      - Vendor
    OpenApi:
      - Vendor
