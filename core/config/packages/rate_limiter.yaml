framework:
    rate_limiter:
        ai_requests:
            policy: sliding_window
            limit: 60
            interval: '1 minute'

when@test:
    framework:
        rate_limiter:
            ai_requests:
                policy: no_limit
